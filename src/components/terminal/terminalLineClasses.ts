/** Monospace command lines (prompt + typed command). */
export const TERMINAL_CMD_CLASS = "text-sm";
/** Plain command output (responses, SSH banner text, errors). */
export const TERMINAL_OUT_CLASS = "text-sm";
